spring:
  # 使用 redis 作为缓存
  cache:
    redis:
      time-to-live: 180000
  redis:
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: 1000